using HeroShips.Modules;
using UnityEngine;

namespace HeroShips.GridMap {
public class GridTester : MonoBehaviour {
    private void Start() {
        // var grid = new Grid<ModuleSlot>(4, 2, (Grid<ModuleSlot> g, int x, int y) => new ModuleSlot(g, x, y));
    }
}
}